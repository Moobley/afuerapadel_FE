# Task 2 Report: Create style.css

## What I implemented
Created `style.css` with full styling for the Torneo Afuera Padel single-page website:
- CSS reset and base typography
- Sticky header with logo, nav links, and hamburger (hidden on desktop via `display: none`)
- Hero section with 3-column grid (logo, diagonal line, title tagline), entrance animations
- Torneo section with section title, description, standings table with sticky header, alternating row colors, hover effect, and `.is-top` highlight
- Footer with responsive grid layout for contacts (email, phone, address, Instagram)
- Responsive breakpoint at 768px: hamburger visible, nav becomes slide-down drawer, hero stacks vertically, diagonal hidden, table padding reduced
- Uses the brand colors: `#231F1E` (dark bg), `#F55A22` (accent orange), `#00A1B3` (cyan for links)

## What I tested
- Opened `index.html` in the default browser to verify visual rendering
- Confirmed dark background, sticky header, hero layout with logo + diagonal + tagline, torneo section, footer contacts
- Hamburger is hidden on desktop viewport
- FadeIn animation on hero elements

## Files changed
- Created: `style.css` (334 lines)

## Self-review findings
None. CSS matches the brief exactly.

## Issues or concerns
None.
